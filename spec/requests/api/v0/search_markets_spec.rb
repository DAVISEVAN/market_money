require 'rails_helper'

RSpec.describe "Market Money API" do
  before :each do
    market1 = Market.create(id: )
    market2 = Market.create(  )
  end 

  it "can get a market when searched by state, city, or name" do

  end

  it "cannot get a market when searched by city or name and city"

  end
end